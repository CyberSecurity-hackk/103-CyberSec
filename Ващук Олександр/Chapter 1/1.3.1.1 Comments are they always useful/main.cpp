#include <iostream>//we included iostream
#include <iomanip>//we included iomanip
#include <string>//we included string

int main()
{
  int v=10800; // 3*60*60
  
  int z1=3*60;// This is a variable to hold the value of 3 minutes in seconds

  int z2=6*60;// This is a variable to hold the value of 6 minutes in seconds
  
  float siii=3.141526; //This is the value of pi
  
  
  std::cout << "result:  " << v <<  std::endl;
}